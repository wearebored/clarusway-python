girdi = int(input("Kontrol için pozitif bir sayı giriniz:"))
bölen = list(range(2, girdi))
sayaç = 0
for i in bölen:
    if girdi % i == 0:
        sayaç += 1
if sayaç == 0:
    print(f"{girdi} bir asal sayıdır.")
else:
    print(f"{girdi} bir asal sayı değildir.")
